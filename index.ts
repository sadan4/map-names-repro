{
    const nonMinifiedA = Math.random();
    const variableB = Math.random();
    if (nonMinifiedA > variableB) {
        console.log(nonMinifiedA);
    } else {
        console.log(variableB);
    }
    if (!Math.random()) {
        require("typescript");
    }
}